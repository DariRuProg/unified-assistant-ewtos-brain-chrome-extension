# {{ vault_name }} — agents.md

## Schema

Dieser Vault wurde mit dem `empty`-Blueprint angelegt. Die Struktur ist absichtlich minimal — nur `inbox/` existiert. Erweitere den Vault über den EwtosBrain Setup-Agent oder lege Ordner direkt manuell an.
